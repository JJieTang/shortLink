# Leave empty to use same-origin /api requests in both Vite dev and nginx-served builds.
VITE_API_BASE_URL=
VITE_API_TIMEOUT_MS=10000
VITE_DEV_HOST=localhost
VITE_DEV_PROXY_TARGET=http://localhost:8080
