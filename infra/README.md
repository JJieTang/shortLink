# Infra Workspace

This directory holds local infrastructure assets that support the ShortLink architecture.

Current reserved subdirectories:

```text
infra/
├── geoip/
├── grafana/
│   └── dashboards/
└── k6/
```

This commit only creates the workspace boundaries so Docker, Grafana, and load-test assets can be added without reshaping the repository again.
