global:
  scrape_interval: 10s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: shortlink-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080
