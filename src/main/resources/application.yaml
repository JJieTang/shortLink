spring:
  application:
    name: shortlink

  datasource:
    url: jdbc:postgresql://localhost:5432/shortlink
    username: shortlink
    password: shortlink

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false